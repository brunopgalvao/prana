<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse-tag">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Prana One", root_path, id: "logo", class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="navbar-collapse-tag">
      <ul class="nav navbar-nav navbar-left">
        <li><%= link_to "Home", root_path %></li>
		<% if signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Schedule <b class="caret"></b></a>
          <ul class="dropdown-menu" role="menu">
			<li role="presentation" class="dropdown-header">Manage Schedule</li>
			<li><%= link_to "Schedule", yoga_classes_path %></li>
            <li role="presentation"><%= link_to "Add a Class", new_yoga_class_path %></li>
          </ul>
        </li>		
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Instructors <b class="caret"></b></a>
          <ul class="dropdown-menu" role="menu">
			<li role="presentation" class="dropdown-header">Manage Instructors</li>
			<li><%= link_to "Instructors", instructors_path %></li>
            <li role="presentation"><%= link_to "Add a Class", new_yoga_class_path %></li>
          </ul>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Account <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li role="presentation"><%= link_to "Profile", current_user %></li>
            <li role="presentation"><%= link_to "Settings", edit_user_path(current_user) %></li>           
          <% else %>
		  	<li><%= link_to "Instructors", instructors_path %></li>
		  	<li><%= link_to "Schedule", yoga_classes_path %></li>
          <% end %>
          </ul>
        </li>
      </ul>
	  <ul class="nav navbar-nav navbar-right hidden-xs">
		<% if signed_in? %>
	  	  	<li class="hidden-xs" role="presentation"><%= link_to "", signout_path, :class => "glyphicon glyphicon-log-out", method: "delete" %></li>
		  <% else %>
		  	<li role="presentation"><%= link_to "", signin_path, :class => "glyphicon glyphicon-user" %></li>		  
		<% end %>
	  </ul>
	  <ul class="nav navbar-nav navbar-right visible-xs">
		<% if signed_in? %>
	  	  	<li role="presentation"><%= link_to "Sign out", signout_path, method: "delete" %></li>
		  <% else %>
		  	<li role="presentation"><%= link_to "Sign in", signin_path %></li>		  
		<% end %>
	  </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
